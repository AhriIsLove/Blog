const TMIComponent = () => {
    return (
        <div className="relative w-full h-full
        max-w-[700px]
        flex flex-col gap-5">
            작업중입니다.
        </div>
    );
};

export default TMIComponent;